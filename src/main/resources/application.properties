spring.application.name=navic-api
server.port=${PORT:8080}

# MongoDB Atlas Connection
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://rutvikagrawaljrf_db_user:rutvikagrawal@navic-cluster.jqhivuy.mongodb.net/navic_db?retryWrites=true&w=majority&appName=navic-cluster}
spring.data.mongodb.database=navic_db

# JWT Configuration
security.jwt.ttl-days=${JWT_TTL_DAYS:7}
security.jwt.secret=${JWT_SECRET:NavICEmergencyResponseSystemSecretKeyForJWT2024ISRO}

# Logging
logging.level.com.example.navic=INFO
logging.level.org.springframework.data.mongodb=WARN

# Server Configuration
server.error.include-message=always
server.error.include-binding-errors=always

# Production optimizations
spring.jackson.serialization.write-dates-as-timestamps=false
management.endpoints.web.exposure.include=health,info